import React from 'react'
import style from './Footer.module.css'

const Footer = () => {
  return (
    <footer className={style.footer}>
        <section className={`container ${style.section_footer}`}>
            
        </section>
    </footer>
  )
}

export default Footer